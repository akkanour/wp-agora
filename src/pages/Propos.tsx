// @pages/Presentation.tsx
'use client';

import { Outlet } from "react-router-dom";

const Propos: React.FC = () => {
  return (
    <div className="">
      <Outlet />
    </div>
  );
};

export default Propos;
